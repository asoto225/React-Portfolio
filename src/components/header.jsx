import React from "react";

const Header = ({ children }) => {
    return (
        <header>
            {/* <h1 className="headerNav">Abel Soto</h1> */}
                {children}
        </header>
    )
}

export default Header;